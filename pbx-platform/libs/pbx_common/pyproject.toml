[project]
name = "pbx-common"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
  "SQLAlchemy>=2.0",
  "asyncpg>=0.28",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["."]
include = ["pbx_common*"]